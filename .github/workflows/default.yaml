name: 'Build and Lint'

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - master

jobs:
  build-and-test:
    name: 'Build and Lint'
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Build and Lint
        run: |
          cd apps/frontend
          npm ci
          npm run lint
          npm run build
          cd ../..

  setup-gcloud:
    name: 'Setup gcloud'
    runs-on: ubuntu-latest

    steps:
      - id: 'auth'
        uses: 'google-github-actions/auth@v1'
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: 'Set up Cloud SDK'
        uses: 'google-github-actions/setup-gcloud@v1'

      - name: 'Use gcloud CLI'
        run: 'gcloud info'

  test-gcloud:
    name: 'Try if gcloud works on next step'
    runs-on: ubuntu-latest
    needs: setup-gcloud

    steps:
      - name: 'try gcloud'
        run: 'gcloud info'
