version: '3.7'
services:
  frontend:
    build:
      context: ./apps/frontend
      dockerfile: Dockerfile
    ports:
      - 8080:8080
  api:
    build:
      context: ./apps/backend
      dockerfile: Dockerfile
    ports:
      - 5555:5555
    environment:
      - PORT=5555
